name: Lint Skills

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    name: Validate skill frontmatter
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Make scripts executable
        run: chmod +x scripts/*.sh

      - name: Run skill validator
        run: ./scripts/validate-skills.sh

      - name: Check for /ck: references in skills/ and commands/ (should be zero)
        run: |
          # Only check skills/, commands/, rules/ - NOT meta docs (CHANGELOG, DEPRECATIONS, etc)
          COUNT=$(grep -rE '(/ck:|"ck:)' skills/ commands/ rules/ workflows/ --include="*.md" 2>/dev/null | wc -l || echo 0)
          if [ "$COUNT" -gt 0 ]; then
            echo "ERROR: Found $COUNT /ck: references in skills/commands/rules"
            grep -rE '(/ck:|"ck:)' skills/ commands/ rules/ workflows/ --include="*.md" | head -20
            exit 1
          fi
          echo "OK: Zero /ck: references in skill/command/rule files"
          echo "Note: Meta docs (CHANGELOG, DEPRECATIONS, README) may keep /ck: for migration history"

      - name: Check skill line counts (max 400)
        run: |
          EXCEEDED=0
          for skill in skills/*/SKILL.md; do
            LINES=$(wc -l < "$skill")
            if [ "$LINES" -gt 400 ]; then
              echo "WARN: $skill has $LINES lines (max 400)"
              ((EXCEEDED++))
            fi
          done
          if [ "$EXCEEDED" -gt 0 ]; then
            echo "WARNING: $EXCEEDED skills exceed 400 lines"
          fi
